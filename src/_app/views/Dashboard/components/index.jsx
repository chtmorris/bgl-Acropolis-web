import React from 'react';
import style from './index.css';

export default class Dashboard extends React.Component {
    render() {
        return (
            <div className={style['root']}>dashboard</div>
        );
    }
}
